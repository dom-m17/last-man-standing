# Types
type Competition {
  id: ID!
  name: String!
  startMatchday: Int!
  status: CompStatus!
}

# Inputs
input CompetitionInput {
  name: String!
  startMatchday: Int!
}

enum CompStatus {
  OPEN
  IN_PROGRESS
  COMPLETE
}

# Responses